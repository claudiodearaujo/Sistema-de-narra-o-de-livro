# Redis Configuration - Environment Variables
# Copy this to .env in the root directory and adjust as needed

# === REDIS ===
REDIS_ENABLED=true
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# === DOCKER COMPOSE ===
# Password for Redis (leave empty for no password)
REDIS_PASSWORD=
